import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Roboto, sans-serif", // Устанавливаем шрифт для всего приложения
  },
});

export default theme;
